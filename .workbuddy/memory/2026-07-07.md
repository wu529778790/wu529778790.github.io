# 2026-07-07 工作日志

## 整理 PDF 为项目笔记
- 用户要求把 Downloads 里最新的 PDF 整理成与项目匹配的 markdown。
- 最新 PDF：`用腾讯刚发布的Hy3手搓顶级动效网站！这4套方案可以封神了【建议收藏】.pdf`（袋鼠帝 AI 客栈文章，讲 workbuddy + 腾讯 Hy3 手搓顶级动效网站的 4 套方案）。
- 用 `pdftotext -layout` 提取文本到 /tmp，按项目 VuePress 风格（front matter: title/date/permalink/categories/tags）整理。
- 产出文件：`docs/09.AI/30.用Hy3手搓顶级动效网站.md`，分类 AI，保留全部技术要点与可复用提示词（含水晶龙提示词、完整主页提示词），剔除公众号推广/公益募捐/上一篇等无关内容。
- 项目约定：docs 下按主题分目录（09.AI、20.笔记 等），笔记用 `---` front matter；AI 类文章放 09.AI。

## 整理第二个 PDF（Cloudflare Workers AI）
- 用户要求把 Downloads 里的 `0元接入 Cloudflare Workers AI.pdf` 也改写成原创口吻。
- 用 `pdftotext -layout` 提取（197 行），整理为 `docs/09.AI/40.0元接入 Cloudflare Workers AI.md`（分类 AI）。
- 内容：为何不接 OpenAI（钱/延迟）→ 接 Cloudflare Workers AI；wrangler.jsonc 配置 AI binding + cf-typegen；env.AI.run 调用示例（垃圾审核/摘要/翻译 m2m100）；免费额度 10000 Neurons/天 及模型对照表；智能留言板完整示例（AI审核→D1存储→AI推荐）；个人网站四次进化总结。
- 已去掉作者署名、日期、阅读数、上一篇/下一篇等公众号运营内容；代码块已清洗 pdftotext 的行号与弯引号。
- 原文 PDF 已移入废纸篓（可恢复）。本次未 push，等用户指令。

## 博客产品问题修复（VitePress）
用户让我以产品视角挑项目问题，并「开始修复」。按优先级修了 P0/P2 三项：

### Fix 1 — 分类归一化（front matter categories）
- 问题：198 篇文章 categories 同义/大小写混乱（笔记/学习笔记/工作笔记、vue/Vue、Http/http、面试题/面试真题/面试突击/八股文、折腾服务器 等），还有 19 篇 hexo 迁移残留元分类 `hexo迁移数据` 直接曝光给读者。
- 做法：写 Python 脚本只改 `categories:` 块（可回退、幂等），同义词/大小写合并；19 篇 hexo 残留按标题逐篇归到真实分类（JavaScript/CSS/面试题/浏览器/前端优化/学习笔记）。
- 结果：分类从 ~45 种降到 23 种，0 个 hexo迁移数据。脚本 `/tmp/normalize_categories.py`。
- 踩坑：5 个文件 `categories: ` 后带空格，正则需容错尾部空白。

### Fix 3 — OG 描述自动提取（config.mts transformHead）
- 问题：0/198 文章写 description，分享卡片/搜索摘要全是同一句站点默认描述。
- 做法：transformHead 缺 description 时读源文件正文首段提取（跳过标题/图片/引用/代码块/列表/表格/容器指令 `:::`、剥离 URL），>120 字截断；仍为空则回退站点描述。
- 结果：构建验证 113/200 篇拿到真实 og:description（空壳/纯链接/代码类文章优雅回退）。
- 附：发现 12 篇 `09.AI/100.Agent学习笔记/*` 是只有 front matter 的空壳文章（162 字节，无正文）——内容质量隐患。

### 小修 — footer 版权
- config.mts footer 补 `copyright: 'Copyright © 2026 神族九帝'` + message。

### 用户主动砍掉的优化项（2026-07-07 晚，明确「删除」）
用户确认以下全部不做，已从计划移除，勿再提议：
- Fix 2 permalink 路由改造（保持装饰字段现状）
- 标签点击化（不做 tag 归档页）
- 关闭 ignoreDeadLinks（保持静默吞死链）
- 接入 analytics（不加埋点）
- 12 篇 Agent 空壳文章（不补内容、不删文件，维持现状）
