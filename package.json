{
  "name": "blog-vitepress",
  "version": "1.0.0",
  "scripts": {
    "generate": "node scripts/generate-posts.mjs",
    "dev": "npm run generate && vitepress dev docs",
    "build": "npm run generate && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "dependencies": {
    "giscus": "^1.6.0",
    "wx-auth-sdk": "^1.2.5"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "comments": {
    "vite vulns": "vitepress 1.6.4 pins vite ^5. Vite vulns (path traversal, fs.deny bypass) only affect dev server, not static production builds. Acceptable risk until vitepress upgrades."
  }
}
